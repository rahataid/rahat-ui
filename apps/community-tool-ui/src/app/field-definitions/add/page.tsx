import AddFieldDefinitions from '../../../sections/field-definitions/add';
import React from 'react';

export const metadata = {
  title: 'Field Definition - Add',
};

export default function page() {
  return <AddFieldDefinitions />;
}
