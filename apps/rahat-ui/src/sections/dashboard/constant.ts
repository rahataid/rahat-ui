export const WARDS = Array.from({ length: 32 }, (_, i) => `${i + 1}`);

// export const MUNICIPALITY = Array.from(
//   { length: 32 },
//   (_, i) => `Municipality ${i + 1}`,
// );

export const MUNICIPALITY = ['LALJHADI', 'BHAJANI'];
