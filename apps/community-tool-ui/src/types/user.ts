export interface IUserItem {
  name: string;
  email: string;
  walletaddress: string;
  role: string;
  status: string;
}

export interface IRoleItem {
  name: string;
  createdBy: string;
  createdAt: string;
}
